apiVersion: v1
kind: Pod
metadata:
  name: windows-demo
spec:
  os:
    name: windows
  securityContext:
    windowsOptions:
      hostProcess: true
  containers:
    - name: app
      image: example.invalid/windows-app
